[workspace]
resolver = "2"
members = ["core", "src-tauri", "slint-app"]
exclude = ["slint-poc"]

# 配布バイナリ（bpsr-app）向け最適化。サイズと速度のバランス＋デバッグ情報除去。
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
panic = "unwind"
